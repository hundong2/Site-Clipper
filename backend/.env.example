# Site Clipper Backend - 환경 변수 예시
# 이 파일을 .env로 복사한 뒤 값을 수정하세요.
# cp .env.example .env

# Gemini API Key (Smart 모드에 필요)
# https://aistudio.google.com/app/apikey 에서 발급
GEMINI_API_KEY=your_gemini_api_key_here

# 디버그 모드 (true/false)
CLIPPER_DEBUG=true

# CORS 허용 오리진 (JSON 배열 형식)
# 프로덕션에서는 안드로이드 앱의 오리진만 허용하세요.
CLIPPER_ALLOWED_ORIGINS=["*"]

# 페이지 크롤링 타임아웃 (초)
CLIPPER_CRAWL_TIMEOUT=60

# 사이트맵 크롤링 시 최대 동시 크롤 수
CLIPPER_MAX_CONCURRENT_TASKS=10
